<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MedPrice.Views.LandingPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MedPrice.Views"
    xmlns:models="using:MedPrice.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="20">
            <!-- Define 5 equal-width columns -->
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Row Definitions -->
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- First input and button -->
            <TextBox Grid.Row="0" Grid.Column="0" x:Name="InputField1" PlaceholderText="Enter first value" Margin="0,0,10,10"/>
            <Button Grid.Row="0" Grid.Column="1" Content="Submit 1" Click="OnSubmit1_Click" Margin="0,0,10,10"/>

            <!-- Second input and button -->
            <TextBox Grid.Row="1" Grid.Column="0" x:Name="InputField2" PlaceholderText="Enter second value" Margin="0,0,10,10"/>
            <Button Grid.Row="1" Grid.Column="1" Content="Submit 2" Click="OnSubmit2_Click" Margin="0,0,10,10"/>

            <!-- Placeholder ListView in column 5 -->
        <ListView Grid.Row="0" Grid.Column="4" Grid.RowSpan="2" x:Name="ItemList"
          Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
          BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
          BorderThickness="1"
          CornerRadius="{StaticResource OverlayCornerRadius}">
            <ListView.Items>
                <TextBlock Text="Example 2: This is a placeholder string." />
                <TextBlock Text="Example 5: This is a placeholder string." />
                </ListView.Items>
            </ListView>
    </Grid>
    </Page>